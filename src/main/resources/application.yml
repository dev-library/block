spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dblock
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  rabbitmq:
    host: localhost
    username : guest
    password : guest
    port: 5672
queuename: COUPON_ATOMIC_QUEUE